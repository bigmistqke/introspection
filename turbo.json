{
  "$schema": "https://turbo.build/schema.json",
  "concurrency": "100%",
  "tasks": {
    "build": {
      "outputs": ["dist/**", "build/**"],
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "tsup*.config.ts"]
    },
    "typecheck": {
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json"]
    },
    "test": {
      "cache": true,
      "dependsOn": ["build", "^build"],
      "inputs": ["**", "!node_modules/**", "!dist/**", "!build/**", "!.turbo/**", "!pnpm-lock.yaml"]
    }
  },
  "globalDependencies": ["tsconfig.json", ".eslintrc*", "jest.config.*"]
}
